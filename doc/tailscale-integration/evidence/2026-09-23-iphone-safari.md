# 2026-09-23 iPhone Safari人工验收

2026-09-23 人工验收恢复：电脑运行真实26.2，HTTPS入口返回200，main.dart.js SHA为26bc49dd7c39e6422963e0fd26215ce088e4aaacf4b1040731a1c120b8a01425，与上一轮已验证候选一致。用户报告手机Safari已发起配对；代理只读确认connected=true、deviceName=safari、pendingPairing=false、hibernating=true。本次没有代理批准动作（检查时配对已完成），尚无本轮提示音听感或后台恢复结果。下一步为网页设置中的声音基线。

用户确认Test sound可听。按引导切换其他App后，用户回复“回来了”（停留时长为引导约20秒，未独立计时）。向当前连接safari发送一次sound=true的“Safari 返回测试 01”；实际横幅与听感等待用户确认，未启用看屏共享。

用户确认“Safari 返回测试 01”显示提醒且听到声音。本轮真实iPhone Safari已通过首次Test sound和切其他App返回后的新有声NUDGE；仅此分项通过，锁屏返回、静音、重复提醒及声音关闭偏好仍待验。

用户在锁屏约30秒再返回Safari的引导后回复“回来了”；实际锁屏时长未独立计时。向当前safari连接发送一次sound=true的“Safari 锁屏返回测试 02”。等待横幅与用户听感结果；不将此测试当作锁屏期间准时投递证据。

用户因走神无法确认测试02的听感，要求重新发送；02不计通过或失败。发送一次新的sound=true“Safari 锁屏返回复测 03”，等待用户确认，使用不同标题避免短时去重影响观察。

用户确认“Safari 锁屏返回复测 03”显示但没有声音。本轮真实iPhone Safari锁屏返回后的新有声NUDGE失败；切其他App返回分项通过不能替代此项。现有网页音频生命周期修复不足以关闭原问题。要求保留现场，不刷新/退出/重连；下一步区分Test sound是否也失声及其显示文案。未宣称已经定位Safari内部根因。

锁屏返回的自动有声提醒03无声后，用户保持原连接进入网页设置，确认手动Test sound可听且显示Test sound played。此次不是旧记录中“Test sound也无声”的相同表现；手势可能恢复或解锁音频，但尚无底层状态证据，不能确认根因。随后发送一次sound=true“Safari 手动测试后复测 04”，检查手动发声后的自动提醒，等待用户结果。

用户确认测试04有声音。证据表明手动Test sound后自动提醒恢复，但尚不能区分浏览器授权、AudioContext恢复时序或输出管线故障；锁屏恢复项仍失败，准备针对性修复。

用户补充：在Test sound和测试04之后，IMF自然触发的ride完成提醒也有声音。当前只观察到一次锁屏返回复测03失声，不能表述为必现或持续失声；后续两次自动提醒成功不能证明下一次锁屏返回已修复。

2026-09-23候选已部署并运行：Chrome自动化33项通过，analyze无问题，Web Release成功，26.2 Gradle build成功且本轮JUnit 66项零失败。构建结束后验证全部49项Web资源逐字节一致、5个nested JAR完整（含monkeycraft-api），备份旧JAR后原子替换。JAR SHA `19ea7e9be774ed3d3d1e1fe7bedddf08a22fb9965b3067b39e52c11cc98945ed`；Web JS SHA `32617edba0a8f442e7e04ee732740a00070a4c89e6560aade5f5994fc1d4a1ae`。旧游戏PID53299正常退出；第一次nohup启动请求没有新进程，45秒等待超时，改为独立会话Popen启动后桥接恢复，真实26.2重新进入mp.imaginefun.net。HTTPS200且实际返回JS与候选一致。当前无客户端连接；接下来只请用户完整刷新手机Safari并连接，真机声音仍待复验。

本轮未修改Java/native音频实现，未重建其余三个Mod或Pages候选，未提交/推送/发布。Sep21的Pages候选归档依然是旧音频代码，不代表本次候选。原生iPhone App园区音频是另一个待验问题。

新候选部署后，用户确认已在手机Safari刷新、重新连接并看到游戏画面。此为用户操作确认；服务端资源此前核对为32617edb，不将其表述为手机远程调试读回。下一步先在当前页面点击Test sound确认声音基线，然后再锁屏返回，期间不刷新、不重连，以验证自动恢复。尚未取得新候选声音验收结果。

新候选真机声音基线：用户确认Reminder sounds已开启，单击Test sound有声音。下一步引导锁屏约30秒后返回同一Safari页面，不刷新、不重连、不再次点击Test sound；待用户返回后再发送新的有声提醒，验证无需额外声音解锁操作的恢复。锁屏期间准时提醒不在本步骤承诺范围。

用户在锁屏约30秒后返回同一Safari页面的引导后回复“回来了”（实际时长未独立测量）。读回唯一连接deviceName=safari后，发送一次sound=true“Safari 新版锁屏返回 05”，工具确认sent=true。等待用户确认横幅及听感，不将发送成功视为声音通过。

用户对“Safari 新版锁屏返回 05”回复“能提到，能看到提醒”，按上下文理解为能听到且能看到。新版第一次锁屏返回后新提醒可听、可见；未单独确认声音次数，不据此认定去重通过。原问题间歇出现，下一步同一页面再次锁屏约一分钟后返回，不点Test sound/刷新/重连，验证连续恢复。

第二次锁屏返回：用户在锁屏约一分钟、返回同一Safari页面且不刷新/重连/Test sound的引导后确认已准备好（时长未独立测量）。核对唯一连接deviceName=safari后，发送一次sound=true“Safari 连续恢复 06”，工具确认sent=true。等待用户确认显示、听感及声音次数，尚不记为连续恢复通过。

用户对“Safari 连续恢复 06”是否显示并只响一声回复“有的”，记录为第二次锁屏返回提醒显示、有声的一次人工确认。新候选连续两轮锁屏返回均可听；仅为本轮样本，不能证明间歇问题永不再现。随后核对唯一连接safari，发送一次sound=false“Safari 静音检查 07”；工具确认已发送，等待用户确认只显示、不发声。

用户确认“Safari 静音检查 07”看到提醒但没有发声，本轮新候选静音消息语义通过。随后核对唯一连接safari，在同一次DebugBridge调用中连续发送两次相同标题、正文、sound=true的“Safari 去重检查 08”；工具确认两次发送调用完成，等待用户确认仅显示一次、响一声。未使用不同标题，未向服务器聊天或其他玩家发送消息。

用户确认“Safari 去重检查 08”符合只显示一次、只响一声的预期，本轮真实iPhone Safari重复即时提醒去重通过。当前新候选声音基线、连续两次锁屏返回、静音消息、重复消息均获用户确认。剩余声音关闭偏好跨后台恢复待验；下一步先请用户在设置关闭Reminder sounds并返回游戏，不在尚未确认关闭前发送偏好测试。

用户确认已关闭Reminder sounds，并明确要求：完成当前检查后，不再对这部分追加测试，除非有非常强的具体理由、经静态验证仍无法保证正常运行。按该指示不再追加关闭声音后的锁屏/后台操作。核对连接safari后，仅发送一次sound=true“Safari 声音关闭检查 09”，用于确认关闭用户声音偏好可压制有声类型的提醒；等待用户确认显示且无声。没有将未进行的关闭偏好跨后台实测记为通过。

用户确认声音关闭检查09显示但不响。新候选32617edb的本轮Safari人工验收到此结束：刷新连接/画面、Test sound、连续两次锁屏返回后的新有声提醒、静音提醒、重复提醒去重、关闭声音偏好均通过。关闭偏好后跨后台、锁屏期间准时投递未做，不记为通过；用户要求不再追加本部分测试，除非静态检查发现具体且重要的风险。手机网页Reminder sounds当前关闭，后续若需要用户正常有声使用可由用户自行恢复，不为恢复设置追加验收。下一待办是原生iOS App园区音频恢复，不能用Safari结果替代。

2026-09-23用户最终验收决定：Safari完整、功能正常，App提醒功能按已通过处理；不在其他Minecraft版本重测，Android最多简短抽查，测试范围删减。TEST_MATRIX已改为简短功能状态表，旧矩阵完整移至evidence/2026-09-23-test-matrix-before-simplification.md仅供追溯；REMAINING_ACCEPTANCE_STEPS及KNOWN_ISSUES_HANDOFF同步，历史长清单不自动恢复为任务。不增加未执行的实测证据。原生园区音乐和平台特有Tailscale问题仍独立收尾。本次仅文档调整，不运行新增测试。
