# P2：26.2 Java 提取器与进程监督（2026-08-30）

范围：仅 `mods/26.2`。Cloth Config UI 未加。默认 `embeddedTailscaleEnabled=false`。

## 已实现

- `NativeHelperExtractor`（JAR SHA-256 sidecar + tmp rename）
- `HelperPlatform`：`darwin-arm64` / `darwin-amd64` / `windows-amd64` 为 bundled
- `HelperProcess`：JSON Lines、nonce、ready 超时、stdout 行长、stderr 脱敏、只杀直接子进程
- `HelperTailscaleService`：`ensureRunning(actualPort)` / `stop` / `logout`
- WebSocket start/stop 与 `CLIENT_STOPPING` 接线
- `/monkey tailscale`、`on`、`off`、`logout`（登录 URL 仅由玩家点击打开）
- JAR 资源：`native/tailscale/{darwin-arm64,darwin-amd64,windows-amd64}/`

## 测试

`./gradlew test` 于 26.2 通过（含 fake helper 进程、extractor sidecar、平台表、脱敏）。
