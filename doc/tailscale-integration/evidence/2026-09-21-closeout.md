# 2026-09-21 自主收尾证据

本轮使用主 checkout `claude/web-m2-m3`，起始314项脏/未跟踪路径均保留。没有 worktree、提交、推送、PR、公开部署、付费资源或实体手机操作。原始记录在 `outputs/closeout-2026-09-21/`，本页仅汇总这一批；同日较早的 Android 验收属于另一批。

## 代码收尾

- 四版 Mod 将认证后的 INFO 安全转发到既有 INFO_PACKET API，限制消息/title/data，并在主线程再次确认连接身份。
- 原生 OpenAudioMc 增加 active 所有权状态，开始加载前报告、清理后释放、传输恢复再报告；显式退出先释放音频再关闭传输。初次加载或 Refresh 失败清理页面及后台租约，允许后续新建页面重试。Web 外部音频不假装持有原生会话。
- 配套修改位于另一个 checkout `/Users/cusgadmin/if-local/imf/` 的 `OpenAudioMcService.java`、`MonkeycraftCompatImpl.java`、启动测试及架构文档。手机持有时真正关闭 PC WebView，抑制自动启动/重试；保留原连接意图和音量。false 才归还，传输丢失不抢占；电脑显式连接可收回。该本地 JAR 同时包含 IMF 原有用户修改，不能把它当作只有本次三处代码的发布补丁。
- 已缩短人工验收页并纠正 P4/Flutter 架构文档的旧独立 Web 描述，保留旧文档快照。

## 自动化与构建

| 检查 | 本轮命令/入口 | 结果 |
|---|---|---|
| Flutter | SDK绝对路径 `flutter test --reporter expanded`；`flutter analyze` | 232通过；无问题 |
| Chrome | `FLUTTER_BIN=… python3 tool/run_browser_tests.py` | 28通过 |
| Java Mod | 各树匹配JDK，`./gradlew spotlessApply build` | 26.2为66，其余各61；0失败/错误/跳过 |
| IMF | `./gradlew test --tests '*OpenAudioMcServiceStartupTest'`，正常build/deploy入口 | 启动类9通过，含两个所有权策略用例；完整构建通过 |
| helper | `bash native/tailscale-helper/scripts/test.sh`（该目录执行） | Go与race通过；macOS ARM64实际执行；5平台构建；Java17/21/25 harness通过 |
| Android | `flutter build apk --release`；apksigner；zipalign `-P 16` | 正常Release通过，v2签名；双ARM ABI共18库；ARM64 LOAD段至少16KiB；32位库单独记录，未声称全18库都是16KiB |
| iOS | `FLUTTER_BIN=… bash tool/build_ios_device_release.sh` | clean构建、所有嵌套Mach-O平台/团队签名通过；1.4.2(11)，未装实体手机 |
| Pages候选 | 普通git archive基线加99文件覆盖；analyze/test/Chrome/build | 无问题、232+28通过、`/monkeycraft/` Release通过；6项provenance工具测试通过 |

日志分别为 `flutter-*-final.log`、各版 `*-final-build.log`、`imf-audio-tests.log`、`imf-build-deploy.log`、`helper-final-tests.log`、`android-final-build.log`、`ios-final-build.log`；候选日志在 `pages-source/pages-*.log`。部分Go结果为缓存，未伪称每项均重新执行。

## 最终产物

完整哈希、字节数见 `artifact-manifest.json` 和 `mod-artifacts.json`。归档位于 `artifacts/`。

| 产物 | SHA-256前缀 | 实际覆盖 |
|---|---|---|
| 26.2 | `8aaaf7e0` | 已重启真实ImagineFun，并用于本轮音频交接 |
| 26.1 | `c2f7664c` | 最终包真实ImagineFun核心回归，60秒467新帧、零解码错误 |
| 1.21.11 | `e0df7343` | 最终包真实ImagineFun核心回归，60秒550新帧、零解码错误 |
| 1.19 | `b42beea2` | 最终包真实MCParks核心回归，60秒487新帧、零解码错误 |
| Android Release | `906884a7` | 构建/结构通过，已保留数据安装模拟器用于MCParks补验 |
| iOS Runner / App.framework | `0c9549f0` / `0cdf8dde` | 正常设备Release归档；尚未真机安装 |
| ImagineMoreFun3.4.2 | `42201a34` | 真实26.2已加载，含配套交接和原用户修改 |

四JAR各49项Web资源与归档root逐字节一致；root与Pages的 main.dart.js 均为 `26bc49dd7c39e6422963e0fd26215ce088e4aaacf4b1040731a1c120b8a01425`，base/入口文件按用途不同。Pages补丁SHA `89011a41552019330b6d73cf224c315abc59525505d472d0a4af414bcbea18da`。99文件范围见 `pages-candidate-paths.txt`，基线为 `ace51c902f906e7ca061e6ee4b5fca94aff9cfb9`，逐文件SHA与当前源码及普通副本一致。provenance标为dirty、commit=null，49项产物哈希匹配，不冒充公开发布提交。

## 真实 Mod 与浏览器

较早同轮库存键修复包先在26.1/1.21.11/1.19分别通过七项核心回归：错误密码、真实H.264、四次resize、E/ESC与Shift blur释放、刷新身份、受控4000重连、60秒视频。新增帧分别582/558/483，零解码错误；各有实际InputHandler的E→E、clickCount=0、按键释放证据。精确包身份在 `legacy-deployment.json`，不转算为随后加入INFO的新包。

最终三版又重跑相同核心回归，见 `final-26.1/`、`final-1.21.11/`、`final-1.19-mcparks-retry/`、`legacy-final-summary.json` 和 `legacy-final-deployment.json`。此前1.19使用既有单机验收世界；用户后来明确允许用 `main.mcparks.us`，不再将ImagineFun最低1.21限制当成1.19无可测服务器。

Pages本地 `/monkeycraft/` +录像协议夹具通过10项检查：登录、解码、resize、输入释放、菜单、静音/声音调用/去重、倒计时更新取消过期抑制、重连、刷新与目标切换凭证隔离。`pages-e2e/result.json` passed=true/errors=[]。无头Pointer Lock为WrongDocumentError（拖动回退），系统通知权限denied所以跳过；不是公开origin、真实Minecraft、系统通知或听感证据。静态服务及夹具进程已结束。

## OpenAudioMc 真正跨端交接

真实26.2 + ImagineMoreFun + iOS26.5调试模拟器，经LAN WebSocket调用真实原生WebView服务。模拟器缺少可打开的Simulator.app并停在系统权限对话框，使用Dart VM调用既有服务、诊断前台标志；不能标为正常GUI、真实锁屏或物理手机结果。运行包包括交接，但随后追加的Refresh失败清理仅由最终232项自动化覆盖，未转算为该运行包结果。

- `audio-phone-ownership-samples-flat.json`：12次/约57秒采样，PC remote=true、active/connected/pending=false、generation=3不增长，原desired=true/volume=0保留。中途关闭4000导致控制连接false→true，PC仍未抢占。
- 手机原生页面 connected=true，后台租约持有，真实detached音乐元素未暂停且时钟推进；No Sleep VIDEO静音。不是仅检查DOM保活视频。
- 明确手机断开后，`audio-desktop-return-samples-flat.json` 13次/约64秒采样，remote=false；约26.4秒电脑connected=true，之后保持，generation=4不变，原意图/音量保留。中途关闭手机控制连接并结束模拟器App没有重新抢占。
- 第一轮归还曾遇服务端结束事件而触发一次恢复，保留早期runtime快照，不承诺瞬间无波动。
- 未认证INFO收到Unauthorized；畸形title/data和非布尔active不改变PC所有权。未验证电脑重启、手机异常退出后的实际组合恢复；进程内显式收回策略已有实现。

## 失败与边界

- **用户截图对应的26.2崩溃**：我在Gradle尚未完成时复制了中间JAR，缺嵌套API导致NoClassDefFoundError。这是本轮部署时序错误，不能归因于内存。保存 `26.2-premature-build-copy.jar` 和 crash.txt；等构建成功、验证nested JAR后原子替换8aaaf7并完全重启，随后真实加入服务器并完成音频测试。后续只从已核验的最终归档部署。
- 1.21.11首次资源包下载未完成时，视频等待失败；保留 `1.21.11-resource-pack-wait/`，进入世界后复测通过。
- 音频新增测试初次受上一fakeAsync测试的静态后台租约队列影响而挂起；增加测试隔离reset后通过，失败/重试日志保留。
- 1.19首次MCParks浏览器脚本没有得到预期新菜单打开事件，停在Game Menu；保存失败目录，不将已有解码100帧冒充全项通过，后续另行复测。
- 本地16KiB检查最初误把所有库当64位、随后又误要求32位依赖全部16KiB而失败；最终分别解析ELF32/64，记录32位实际对齐，只对ARM64作16KiB断言，APK ZIP对齐独立通过。
- Live Activity本地通知到点不执行Activity.end；App挂起时卡片可能停00:00。恢复清理/显式取消与到点通知分开验收，不暗中增加APNs后端。
- 物理iPhone Safari提示音恢复、原生音乐恢复、最终包去重/计时、Android本人tailnet授权与真机/OEM后台、旧版节点授权、跨网络/DERP/撤销及Windows/Linux原生联网仍未完成。见 [最短人工步骤](../REMAINING_ACCEPTANCE_STEPS.md)。

## MCParks补验

用户明确提供 `main.mcparks.us` 并允许代理继续接管1.19。Android API36 ARM64 AVD使用SwiftShader、保留数据升级最终APK，经普通GUI连接真实1.19；服务器原始audio链接由已安装 `my-mcparks-experience-1.0.0.jar` 适配器重写为mcparks-v1，点击后App设置显示Connected to MCParks、50%音量、一条area循环曲目，媒体前台服务ID990002正常。既有MCParks适配器源码只读检查，未修改或部署该仓库。

- Home后台111.665秒，App进程3655不变，媒体前台服务仍在，系统AudioPlaybackConfiguration显示此UID/PID的AAudio player处于started。返回正常GUI仍为Connected和一条实际曲目。
- 音量设置从50%改为24%，再恢复50%；记录的是UI设置和调用行为，没有测量实际声压。单曲Stop经历淡出后显示nothing playing；Refresh后重新Connected并收到area曲目。
- 模拟器显示状态Asleep保持68.471秒，期间播放器仍started、服务仍foreground；唤醒返回无需Refresh仍Connected和一条曲目。此处是模拟器熄屏，不是实体手机/OEM锁屏策略。
- 显式Disconnect后媒体前台服务消失，再Home/返回音频区域未复活；系统不再有App的started播放器。电脑MCParks音频原本未连接，收尾仍false，不把这次当成电脑已有音频的交接测试。
- 结构化断言 `mcparks/result.json` 全通过；GUI XML、dumpsys与时间数据均在同目录。没有人工听感确认。初期忙碌聊天不断滚动，几次定位点击没有取得连接证据；最后使用新收到的真实audio链接完成普通GUI连接，未通过调试接口强制启动服务。

1.19第一次浏览器菜单检查时，失焦自动暂停为true并出现Game Menu，不满足脚本“从关闭菜单开始按E”的前提；保留失败。重测暂时关闭此设置后七项核心检查通过，60秒487新帧、零解码错误；完成后恢复true。此结果不证明默认失焦暂停环境下可以无条件省略游戏菜单前提。

## 收尾状态

已正常退出本轮启动的游戏、Android与iOS模拟器；ADB无设备，simctl无booted设备。最终1.19停止前client=false、Shift=false、inventoryDown=false、clickCount=0，pauseOnLostFocus恢复true，电脑MCParks音频保持原off。调试OpenAudioMC会话临时文件已删除，模拟器userdata保留，空闲Gradle守护进程已停止。没有实体手机安装或后台操作。精确读回见 `final-restored-state.json`，安装的Android APK读回SHA与归档相同（`mcparks/installed-apk.json`）。

原iOS音频兼容、手机Safari恢复提示音、最终iOS通知去重、账户/跨网络/平台范围及Pages公开发布仍按前文待办，不能因MCParks模拟器通过就全部勾选。下一步由用户回来后逐项完成两组手机测试；不需要重做已通过的旧版基础回归。
