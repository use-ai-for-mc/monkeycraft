# Flutter Web → 真实26.2验收

2026-09-19，本轮用户明确交出ImagineFun Add-Ons实例并授权代理按需正常重启。使用主checkout，没有创建worktree、提交、推送或部署Pages。

## 环境与产物

macOS ARM64；真实Minecraft 26.2、ImagineFun原服务器；Flutter 3.41.2/Dart 3.11.0；实际Chrome 153.0.8010.53（长测为headless）和Safari WebDriver。地址为本机已配置的tailnet HTTPS/WSS入口，不忽略证书错误、不改DNS或网络访问范围。

| 版本 | 最终JAR SHA-256 | 本轮证据边界 |
| --- | --- | --- |
| 26.2（当前 overlay 包） | `ee2dbc284e7865dcc06a6a16e524570302f03329afa7dbb373d40a65254dffd9` | 已正常重启并回到 ImagineFun 原服务器；JAR 内 49 个 Web 资源与 Flutter root 构建逐字节一致，HTTPS 取回的 `index.html`、`main.dart.js`、`flutter_bootstrap.js`、`reminder-sw.js` 也与该构建一致 |
| 26.2（倒计时历史包） | `471ff457d13e19d2a70108555aeacc28baaf0ffb281608c780a5935a25eff438` | 已部署及 49 个 Web 资源一致的历史记录；不得转算为当前 overlay 包的实时结果 |
| 26.2（Chrome十分钟历史） | `0f0bc8f66dcd43ba8cc5cdea418a4bfb168b9c0419380d15ddec7d9d0c92fbbf` | 下文Chrome实时结果只对应此历史包，必须保留其版本边界，不能转算为当前构建的通过结果 |
| 26.1（当前 overlay 包） | `660d0a5008704e6cb84c72618188cd53767861419a17a143220556f044a5c389` | 构建、61 项 JUnit（0 failed/error/skipped）及 49 个 Web 资源逐字节核验通过；已在真实 ImagineFun 完成60秒回归，随后已恢复运行26.2 |
| 1.21.11（当前 overlay 包） | `0063d2898f497a7907b9207ec0bd3c3853c13db41d8ec087c81278c6d013aa97` | 构建、61 项 JUnit（0 failed/error/skipped）及 49 个 Web 资源逐字节核验通过；真实 ImagineFun 60秒回归通过，随后恢复运行26.2 |
| 1.19（当前 overlay 包） | `52fa89f6cf082e45b045fb582ccddfa963e3f59bfd268227575756d05db7b48f` | 构建、61 项 JUnit（0 failed/error/skipped）及 49 个 Web 资源逐字节核验通过；在独立测试世界完成60秒回归，随后恢复运行26.2 |

当前 overlay 26.2 包已正常退出旧进程后原子替换，并用原 Prism 实例和已有账户重启并回到原服务器。`overlay-deployment.json`记录其 SHA 为`ee2dbc…`及 49 个匹配 Web 文件；HTTPS 资源采样中的 `main.dart.js` SHA-256 为`57a79829ad188aa530b9ef61e1d3a27dd8a273078822d4939c2c6e169d92e39b`。旧包保留在本地证据目录，含密码的完整配置备份位于仓库外私有临时目录。Chrome十分钟长测仍仅对应表中的`0f0bc8…`历史包；当前 overlay 包不能据此转算为已完成Chrome验收。

当前 overlay 包的真实 collision 已通过：deadline `1789792405709` 在 `5712` 出现，到期后82ms收到普通 NUDGE、精确可见下界为2634ms，另一次 ImagineMoreFun NUDGE 也未覆盖到期提醒；decoded=190、errors=0。此前约81ms会覆盖提醒的现场记录保留为修复前对照。该用例只证明至少2634ms可见，不写成已实测12秒。

完整 Chrome timer 生命周期也已通过：页面静音横幅、同 deadline 的 A→B 更新、取消、到期横幅与倒计时移除后至少2秒持续可见；decoded=308、errors=0。此现场 `Notification.permission=default`、tones=0，只证明页面静音路径，不证明 OS 通知或可听声音。

当前 overlay 包的 Chrome 核心120秒回归通过：8个15秒样本新增帧为120、120、122、119、124、120、122、122，合计969，解码错误0；socket总数保持2，且该数目只含受控服务端4000关闭后的一次恢复，没有额外重连。错误密码被真实Mod拒绝；正确密码认证与H.264解码、resize后decoder/socket保持、E/ESC、短Shift按下后的blur释放、同目标身份刷新恢复、服务端4000关闭后的恢复均通过。MCP在菜单暂停时确认InventoryScreen并有截图，断线前确认screen=null/Shift=false。三个旧版当前 overlay JAR 均随后完成真实60秒回归：26.1四个15秒样本共521帧，1.21.11在ImagineFun共562帧，1.19在独立测试世界共479帧；每项七个检查全过、`errors=[]`、decoder错误均为0。随后已恢复26.2 ee2dbc原服，helper为running/listening，root确认screen=null、Shift=false、ridingSleep=true且无连接。

Chrome认证后配置只正常更新lastPhoneName/lastPhoneSeenAt；不能称整份配置SHA未变。密码、配对身份与用户设置保持原值。为测试乘坐时的视频，IMF的hibernationWhenRiding仅在内存临时设false，磁盘仍true；测试结束要恢复true。

## Chrome实时结果

- 错误密码被真实Mod拒绝；正确密码认证并解码真实H.264。
- 四次浏览器viewport变化后继续解码，decoder/socket保持同一实例。
- E后等待新帧，截图确见背包；MCP独立确认InventoryScreen。ESC后收到本次SCREEN_STATE=false，受控断线前MCP再次确认screen=null。
- 短Shift按下后触发浏览器blur，实际发送释放；游戏端确认Shift=false。
- 刷新恢复同目标身份，不重填密码即可连接。
- 真实server socket以4000关闭（不发送表示主动退出的DISCONNECT），客户端新建连接并恢复至少12帧。
- 之后连续600秒、40个15秒区间全部有新帧，共4878新帧，单区间114–136帧，解码错误0，socket计数不变。配置960x600/10FPS，不能把它写成20FPS验收。
- Java RSS采样40次，约2993–3762MiB，结束约3015MiB，未见单调增长；这不是heap分析或长期无泄漏保证。

长测期间服务器报告过原生界面开/关，刷新后浏览器没有发送任何INPUT/SCREEN_KEY；末尾截图为正常背包。因此本轮连续解码覆盖世界及菜单画面，不声称600秒一直是无菜单世界场景，也不凭此归因本地界面变化来源。

首轮截图过早，SCREEN_STATE先于新H.264画面。代理主动中断并改取证为至少5个新解码帧后截图；保留首轮interrupted记录，没有把它计为完整长测或改动产品绕过问题。

## Safari实时结果（按包区分）

历史`0f0bc8…`包的 attempt3 已通过真实HTTPS认证、H.264连续解码、三次resize、E/ESC和静音横幅；E有网页截图与MCP InventoryScreen双重证据。初轮E因测试窗口焦点条件未满足失败；加入实际document.hasFocus检查及flutter-view focus后通过。`471ff457…`倒计时包只有真实AX倒计时截图；旧selector漏检，后续焦点/登录失败，不能把该截图扩展为完整Safari流程通过。

当前`ee2dbc…`包的 Safari WebDriver 真实HTTPS/WSS流程已通过，原始记录为`outputs/roadmap-2026-09-19-pages-safari/flutter-safari-live-acceptance.json`。脚本以JS语义点击 Connect 时未建立 socket；随后 root 用原生 CUA 可访问性树点击 Connect，实际应用成功认证并持续解码（最终decoded=609、received=626、errors=0）。这属于自动化点击机制差异，并未修改应用。三次窗口resize后均继续解码；E打开后MCP独立确认`InventoryScreen`，ESC关闭，随后150ms Shift按下/释放对已发送且最终Shift=false。最初“再解码五帧”截图仍是旧世界像素，不能作为背包视觉证据；后续原生 CUA 截图及独立保存的WebDriver截图才可见背包，见`flutter-safari-live-inventory-confirmed-later.png`，与MCP证据相互印证。

同一真实Safari会话还通过页面静音横幅、同deadline的A→B更新、取消、到期倒计时及倒计时移除后横幅至少持续2秒。后续真实前台交互记录`flutter-safari-interactive-acceptance.json`显示，在`hasFocus=true`和`connected=true`时请求Pointer Lock，收到locked=true，Escape后收到locked=false；仅发送一个`LOOK_DELTA`，未发送CLICK。原生 CUA 坐标动作会触发Safari自动化玻璃保护提示，已点Continue Session但未关闭自动化；最终锁定用Safari WebDriver的W3C鼠标动作完成，不写成纯人工鼠标验收。

声音记录`flutter-safari-interactive-sound.json`显示前台、permission=granted时tones从8开始，静音NUDGE后仍为8，有声真实NUDGE后为9，重复有声NUDGE仍为9，说明该次调用及去重路径被记录。交互结束时tones为12，但其他ImagineMoreFun自动提醒也会调用音效，不能把全部增量归因于这一次测试，更不等于用户已经听到声音。新tab尝试仍为`document.hidden=false`，其NUDGE只属于前台；会话正常关闭，尚未验证后台或系统投递。`secureContext=true`、`Notification.permission=granted`和Service Worker存在也不等于系统投递或听感；用户听感问答仍待完成。上述真实服务验收与Pages本地录像夹具E2E是不同环境，不能互相替代。

## 独立构建与候选发布验证

主checkout完整Flutter 224 项测试与analyze均通过。当前 overlay 26.2 已真实部署、重启并回到原服务器；26.1、1.21.11 和1.19均完成本轮`./gradlew build`，每个目标的 61 项 JUnit 均为 0 failed/error/skipped，且各最终 JAR 的49个web资源均与 Flutter root 构建逐字节一致。三个旧版当前 overlay 都已完成上述真实运行回归，最后恢复26.2。

73文件Pages候选（含 overlay 修复）已在无 `.git` 的独立普通 archive 中完成验证：逐文件 manifest SHA-256 匹配、依赖锁、analyze、204项VM/widget（其中 overlay 5 项）、24项真实Chrome、`/monkeycraft/` Pages Release及发布树检查均通过。完整结果与日志索引见`outputs/flutter-live-26.2-2026-09-19/pages-candidate-overlay-validation/RESULT.md`。该候选验证说明当前发布候选的自动化结果，不能把Chrome的24项自动化验证或历史十分钟实时长测转算为Safari通过，也不表示所有平台、公开Pages入口或OS通知/可听声音验收已最终完成。

该候选的Pages Release还完成独立loopback端到端：`/monkeycraft/` base 由本地静态服务加载，Flutter共享客户端仅连接 `web/tools/replay-server.ts` 的录像夹具，覆盖错误密码、实际录像H.264、resize、提醒音效调用去重、到期取消/过期重放抑制、4000关闭重连、刷新与改目标清除凭证。无头鼠标锁定与OS通知权限未通过环境能力，分别只记录拖动回退和跳过，未迁移为系统或前台验收。结果见`outputs/flutter-live-26.2-2026-09-19/pages-overlay-e2e/RESULT.md`；未连接真实Minecraft。

当前脏共享Flutter源码的原生补充：首次直接iOS Release虽在50.6秒完成，但`objective_c.framework`为Simulator平台，设备包验证失败，失效包与日志已保留。随后仓库清洁wrapper重建通过逐嵌套Mach-O及签名验证，得到可用于设备的Release产物（Runner `369feb4e4f8c918e2d69d22e35fa3be7242a211a98113618d7d7ed67408d44c6`，App.framework `0466933f0777371936623c2863b742eba599a7613fb946a7030af7e0924a3473`）；物理iPhone不可发现，未尝试离线安装或readback，不能称为本次iPhone真机通过。Android Release APK`39218eaa4542dce00410e748f3882f52275b4a879c6adaa11b695d879afe5f6f`（83,686,326字节，双ABI、Tailscale库、个人签名、v2及16KiB对齐）已用`adb install -r`安装到保留用户数据的Android 16/API 36 arm64 AVD，并直连真实26.2。系统通知许可、静音NUDGE通知栏投递、计时A→B单alarm、取消、到期系统通知均通过；展开通知栏暂停流和WebSocket后，同一MainActivity回前台恢复，受控4000关闭也恢复连接，触控Sneak释放后Shift=false。硬件E打开真实InventoryScreen，但自动化路径的后续硬件E/ESC未关闭它，属于部分通过而非完整硬件键切换。模拟器已关闭且未清数据；未登录或授权Tailscale，模拟器没有可听声音结论。较早debug APK的SHA为`e7249ee27d06263eabc1dc1c1dd29815552923ca0b7ee5094c722b520a82e5f0`。详细证据见`outputs/flutter-native-overlay-2026-09-19/android-roadmap-release-runtime.md`和`outputs/flutter-native-overlay-2026-09-19/RESULT.md`。

## 原始证据与待办

本地根目录：`outputs/flutter-live-26.2-2026-09-19/`。Chrome命令参数由`flutter/monkeycraft/tool/browser_live_smoke.mjs`读取，密码仅从本机配置进程内读取；result/checkpoints/screenshots、native menu证据、重启与资源清单、内存采样、所有构建日志均保留。

当前`ee2dbc…`的桌面Safari认证、键盘、三次resize、完整timer生命周期和前台Pointer Lock已完成；前台有声调用与去重有计数证据，但用户听感、后台状态和系统通知投递仍待验证。Android 16 AVD的最新Release已完成上述受限运行，仍不是实体Android听感或Tailscale授权验收；实体iPhone Safari/Android Chrome、本轮最新iPhone包行为及公开Pages入口仍不因本次编译或桌面结果转为通过；73文件Pages发布范围的授权问答也仍待完成。原生App和旧版独立Web历史证据保持原有版本边界。
