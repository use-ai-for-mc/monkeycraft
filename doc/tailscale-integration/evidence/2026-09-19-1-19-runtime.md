# 2026-09-19 1.19 运行验收

本文前半部分记录早期 `a64bd9…` 包的短测；后续版本和长测结果单列，不覆盖历史证据。

## 部署与预检

本轮运行 PrismLauncher `Fabric 1.19` 实例，Java 为 17.0.15。实际部署 `monkeycraft-1.4.2-1.19.jar` 的 SHA-256 为 `a64bd937045ee626845cd60f183206ab7231a63c459b2842f6c7082e361248ec`；部署记录确认未触及其他 Mod。

原 MonkeyCraft 配置在运行前后的 SHA-256 都为 `db8e5b9868c094a0188cf5431cea607f109c09448c637d376145eab64c4f4aac`。本轮没有更改 MonkeyCraft 开关、代理配置或账户设置。

## 服务器兼容性边界

首次直接连接服务器时，既有 ProxyServer Mod 因未经过 Multiplayer 界面初始化，`proxyMenuButton` 为空并在 `initChannel` 抛出 NullPointerException。通过已有 UI 执行 Cancel 回到标题页，再打开 Multiplayer 后，`proxyButtonInitialized=true`；未更改任何 Mod 或配置。

随后对同一服务器的重试得到明确拒绝：服务端要求最低 1.21+，拒绝 1.19。因此本证据不将 1.19 计为 ImagineFun 服务器兼容性验收。

## 本地世界验收

通过正常 Singleplayer UI 新建了独立 Creative/Superflat 世界。早期记录将名称写为 `MonkeyCraft acceptance 2026-09-19`；本轮复读 `level.dat` 和游戏列表确认实际名称与目录均为 `MonkeyCraft acceptance 2026-09-1`；`mc_wait_until_in_world` 成功。该测试世界被保留，供后续复验。

在此本地世界中，实际 HTTPS 视频及 E/Escape 用例通过：浏览器实时用例为 1 项通过、十分钟 soak 未请求而跳过。视频调试统计为 `recv 22`、`dec 21`、`err 0`、`cfg 1`，连接状态为 `connected`，协商尺寸为 `750x524`。

输入释放通过真实会话验证：按住 Shift 时连接保持且 Shift 为 `true`；关闭浏览器标签页且未显式 key-up 后，连接为 `false`、Shift 为 `false`。

## Helper 生命周期

未修改默认开关、也未执行账户授权。helper 从 `stopped` 冷启动到 `needsLogin`，错误代码为空，且未认证。退出 Minecraft 后，Minecraft PID 59347 和 helper PID 61210 均已退出，没有残留进程。

## 资源与限制

打包内的 `index.html`、通知 worker 与 JS/CSS 资源均与当前 `web/dist` 匹配。结果仅覆盖本地 1.19 世界中的功能路径；不代表 ImagineFun 服务端支持 1.19、Tailscale 授权成功、十分钟稳定性，或真机验收。

原始产物位于 `outputs/roadmap-2026-09-19-version-runtime/`：`1.19-assets.json`、`1.19-server-attempt.json`、`1.19-runtime.json`、`1.19-live.log`、`1.19-input-held.json`、`1.19-input-closed.json`、`preflight.json` 和 `local-deployments.json`。

## 后续十分钟持续验证（dd3dd0 包）

随后部署的 `dd3dd059c12248bcd41a01a0e991fc3dc1cce418ba6ca0a4aea9cd8866f1c215` 包，在 Java 17.0.15 / 游戏 PID 89366 上复用了上述独立本地世界，没有新建或操作其它存档。通过实际网页设置请求 20 FPS，以真实 HTTPS/WSS 运行普通视频用例与十分钟持续用例，两项通过。持续 600,030 ms，四次尺寸变化，最终 `dec 7971 / err 0 / cfg 6`；六个采样点连接均为 connected，错误均为零。请求值不等于实达值，最终瞬时采样为 13 FPS。

浏览器结束后连接与 Shift 均为 false，游戏正常退出且端口关闭，原 MonkeyCraft 配置 SHA-256 仍为 `db8e5b9868c094a0188cf5431cea607f109c09448c637d376145eab64c4f4aac`。此次未授权该实例的内嵌节点，未执行跨设备或真机验证。

该包含 PointerLock Promise 清理修复；后续旧式 void 返回接口的修复另记产物，不把本次长测自动转算到后续哈希。原始日志、六点采样、截图和清理记录分别见 `outputs/roadmap-2026-09-19-version-soak/1.19-live.log`、`1.19-result.json`、`1.19-playwright/`、`1.19-cleanup.json`。

## 最终 PointerLock 包短测与背包复验

最终 JAR `e9df6d39e3ad9e2614550f4c717559a370857e2e58059f1926df20c6e8c843bc` 在 Java 17.0.15、游戏 PID 62193 上复用既有世界 `MonkeyCraft acceptance 2026-09-1`。标准浏览器用例在 3.5 秒内通过，确认真实 HTTPS 视频解码超过 20 帧且错误为 0；该轮共 1 项通过、1 项跳过，跳过的是未请求的十分钟 soak。

初轮 `1.19-playwright/live-stream.png` 与 `live-inventory.png` 实际都显示 `Game Menu`。旧用例看到通用 `screen-palette` 已存在后跳过了 E，再用 Escape 关闭菜单也能通过，因此该轮不能计为背包证据；同一断言限制也适用于更早 `a64bd9...` 与 `dd3dd0...` 包的普通用例，不影响其视频和十分钟持续结果。加强用例先关闭既有 Screen、聚焦视频区域、始终发送 E，并等待 Screen 重新打开和至少四个新解码帧。重跑为 1 项通过、1 项跳过，总计 5.8 秒；`1.19-inventory-recheck/live-inventory.png` 清楚显示 Creative Inventory 的 Building Blocks 分类、物品网格和物品栏，最终包的背包打开与关闭由此通过。

独立输入释放探针在按住 Shift 时观测到 `connected=true`、`shift=true`；未显式发送 key-up 而直接关闭浏览器后，两者均变为 `false`。本次没有重复启动 helper，游戏正常退出，配置 SHA-256 仍为 `db8e5b9868c094a0188cf5431cea607f109c09448c637d376145eab64c4f4aac`。这些短测和释放结果归属于 `e9df6d3...`；前述 600,030 ms 十分钟结果仍只归属于 `dd3dd0...`，没有转算到最终包。原始结果位于 `outputs/roadmap-2026-09-19-26.1-acceptance/` 下的 `1.19-result.json`、`1.19-live.log`、`119-held.json`、`119-closed.json`、`1.19-inventory-recheck.log` 与 `1.19-inventory-recheck/`。

## Final overlay 60-second regression

Final JAR SHA-256: `52fa89f6cf082e45b045fb582ccddfa963e3f59bfd268227575756d05db7b48f`; retained predecessor: `e9df6d39e3ad9e2614550f4c717559a370857e2e58059f1926df20c6e8c843bc`. The replacement was copied, ZIP-validated, SHA-256-compared, and atomically renamed. Configuration hashes were retained before and after deployment and after the session; this run changed no configuration, node identity, helper, or HTTPS/WSS service.

The client opened only the existing local acceptance world `MonkeyCraft acceptance 2026-09-1`; no incompatible remote server was attempted. The headless browser passed rejected invalid credentials, actual H.264 decoding, four resize transitions, fresh E/ESC, Shift blur release, reload/reconnect, and code-4000 authenticated-session-close reconnect. MCP confirmed a freshly opened `CreativeModeInventoryScreen` with item-picker menu. The 15/30/45/60-second decode deltas were 120, 120, 120, and 119 with zero decoder errors. Browser and game then exited normally. Evidence is under `outputs/flutter-live-26.2-2026-09-19/legacy-overlay-runtime/1.19/`.
