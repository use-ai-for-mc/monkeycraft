# 2026-09-19 1.21.11 运行验收

## 部署与预检

本轮运行目标为 PrismLauncher `ImagineFun` 实例，使用 Java 21.0.7 和已部署的 `monkeycraft-1.4.2-1.21.11.jar`。部署 JAR SHA-256 为 `500105bac88d5a50d4e2f20a1e73f2281ebe7d859fc113eb2d48eea73bd07dde`；预检记录确认仅替换该 JAR，其他 Mod 未触及。

运行前配置启用本地网络、端口 9600、服务在标题页自动启动，且保留既有配置。运行结束后配置 SHA-256 仍为 `3f6c222c0efdcecb12542ff768abf0471f1d633f1e19572b0cff022009843f9c`，与预检相同。

## 实测结果

真实 1.21.11 已进入同一 `mp.imaginefun.net` 会话。浏览器经开发代理连接后，视频测试通过：实际 H.264 解码统计为 `recv 22`、`dec 21`、`err 0`、`cfg 1`，协商尺寸为 `750x524`，连接状态为 `connected`。标准实时浏览器用例结果为 1 项通过；十分钟 soak 未在此版本请求，因此跳过。

输入释放以真实会话验证：按住 Shift 时连接保持且 Shift 为 `true`；关闭浏览器标签页、未显式发送 key-up 后，连接变为 `false`，Shift 变为 `false`。该结果来自 `1.21.11-input-held.json` 与 `1.21.11-input-closed.json`。

嵌入式 helper 在未修改配置下从 `stopped` 进入 `needsLogin`，未认证且没有错误代码。退出 Minecraft 后，Minecraft PID 57735 与 helper PID 58803 均已退出，证明此轮 helper 没有遗留进程。

## 首次尝试

首次浏览器用例在世界尚未准备好时启动：45.1 秒等待后仍处于 `ConnectScreen`，稍后另一次等待 26.1 秒才确认进入世界。该次的视频断言已经通过，但 Escape 后调色板仍存在，导致库存往返断言失败；因此它不构成在世界内的库存交互验收。

原始首轮日志因备份命令使用了错误工作目录而被覆盖。`1.21.11-initial-attempt.json` 保留了工具捕获的失败结果和这一限制；本证据不声称原始日志仍然存在。

## 未覆盖范围

本轮未进行旧版本的内嵌本人授权验证、十分钟 soak、或真机复验。`needsLogin` 仅证明 helper 生命周期和未认证状态，不代表 Tailscale 账户授权或真实远端 peer 连接成功。

## 原始产物

结果来自 `outputs/roadmap-2026-09-19-version-runtime/` 下的 `1.21.11-assets.json`、`1.21.11-runtime.json`、`1.21.11-initial-attempt.json`、`1.21.11-live.log`、`1.21.11-input-held.json`、`1.21.11-input-closed.json`，以及 `preflight.json` 和 `local-deployments.json`。

## WindowMixin 与 20 FPS 后续阶段

后续 JAR `d42a65b35cade5019f7a9999a79fc948d74493c2e2f894ae5861c757c5a8a9b3` 已部署，用于消除 1.21.11 原版在窗口最小化时的 10 FPS 图标化限制。请求 20 FPS 的两项真实浏览器用例通过并持续 600,031 ms，最终为 `dec 6778`、`err 0`、`cfg 6`，四次尺寸切换后连接仍保持。

最小化的 140,010 ms 区间解码 1,589 帧，平均 11.349 FPS；这证明流继续运行，但不能等同于达到请求的 20 FPS。原生窗口图标化时 mixin 报告 false，且其它帧率限制仍可使实际 limit 为 30。恢复窗口后确认原生图标化为 0。正常清理后游戏进程退出、连接和 Shift 均为 false，配置 SHA-256 保持原值。旧 `500105...` JAR 的 10 FPS 基线保留为历史对照。

## 最终 PointerLock 包短测

最终 JAR `b927c80ffbab19a87ee648c7a3c3758eea9cf32919aacc942d825c6a044104a9` 已在真实 `mp.imaginefun.net` 会话启动，Minecraft PID 为 61319。标准浏览器实时用例在 3.7 秒内通过；用例确认画面解码超过 20 帧且错误为 0。该次运行共 1 项通过、1 项跳过，跳过项是未请求的十分钟 soak。

截图复核后不把该次通过计为背包往返证据：`live-inventory.png` 没有显示背包槽位或可识别的背包界面，只能看到调试状态从 `screen closed` 变为 `screen open`。现有用例在通用 `screen-palette` 已可见时不会发送 E，而该组件只证明某个 Minecraft Screen 已打开，不能证明它是背包。最终包的背包打开与关闭需由加强后的用例重新验证。

独立输入释放探针在同一最终包上按住 Shift 后，服务端观测为 `connected=true`、`shift=true`。探针未显式发送 key-up，直接关闭自己的浏览器后，服务端变为 `connected=false`、`shift=false`。本次没有重复启动 helper；游戏随后正常退出，配置 SHA-256 仍为 `3f6c222c0efdcecb12542ff768abf0471f1d633f1e19572b0cff022009843f9c`。

本次结果只证明 `b927c80...` 最终包的短时真实视频和输入释放；背包操作待加强复验。前述 600,031 ms 十分钟与最小化结果仍只归属于 `d42a65...`，没有转算到最终包。原始结果为 `outputs/roadmap-2026-09-19-26.1-acceptance/1.21.11-result.json`、`1.21.11-live.log`、`12111-held.json` 与 `12111-closed.json`。

### 加强后的背包复验

加强用例先关闭既有 Minecraft Screen、聚焦视频区域、始终发送 E，并在 Screen 重新打开后等待至少四个新解码帧再截图。`b927c80...` 包按该流程重跑为 1 项通过、1 项跳过，总计 6.0 秒；跳过项仍是未请求的十分钟 soak。`1.21.11-inventory-recheck/live-inventory.png` 清楚显示 ImagineFun 蓝色背包槽位、角色预览和物品，因此最终包的背包打开与关闭现已得到实际画面证据。前一段误判限制继续保留，记录旧用例为什么不能单凭通用 `screen-palette` 得出背包结论。

## Final overlay 60-second regression

Final JAR SHA-256: `0063d2898f497a7907b9207ec0bd3c3853c13db41d8ec087c81278c6d013aa97`; retained predecessor: `b927c80ffbab19a87ee648c7a3c3758eea9cf32919aacc942d825c6a044104a9`. The replacement was copied, ZIP-validated, SHA-256-compared, and atomically renamed. Configuration hashes were retained before and after deployment and after the session; this run changed no configuration, node identity, helper, or HTTPS/WSS service.

The real client joined `mp.imaginefun.net`. The headless browser passed rejected invalid credentials, actual H.264 decoding, four resize transitions, fresh E/ESC, Shift blur release, reload/reconnect, and code-4000 authenticated-session-close reconnect. MCP confirmed a freshly opened `InventoryScreen` with `InventoryMenu`. The 15/30/45/60-second decode deltas were 138, 141, 142, and 141 with zero decoder errors. Browser and game then exited normally. Evidence is under `outputs/flutter-live-26.2-2026-09-19/legacy-overlay-runtime/1.21.11/`.
