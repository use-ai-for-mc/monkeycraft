# 发布前剩余事项

**当前最高优先级范围（2026-09-23用户更新）：只处理和验证直接属于本次Tailscale或新网页端的改动；其余功能按无实质变化、此前正常则继续正常处理，沿用既有结论，不排查、不补测、不列发布门槛。这是验收范围决定，不是声称Git没有其他差异，也不撤销已有代码。范围内仍只确认主要功能，次要功能不测试；已完成的Safari不重测。**

更新时间：2026-09-23。按用户决定：主要功能正常即可，次要功能不再测试；默认不需要用户听声音或配合手机操作。

**明确删除：Windows/Linux原生联网复测，以及原生App的声音播放、听感、静音、锁屏/Home返回检查。无具体回归证据时沿用此前正常结论，不以少量代码改动或环境覆盖不足为由重新列入待办。**

## 代理自行完成

1. 将已验证的共享Web资源同步到四个Mod和Pages候选，核对包内容；不按Minecraft版本重复验收。
2. 仅在本次Tailscale交付需要时整理iOS/Android包及必要版本、签名信息；不为原生音频、通知或其他范围外功能重建、安装或安排验收。
3. 准备具体、可审阅的发布范围与必要说明。不因一般性担忧添加更多检查。

## 只有这些情况需要用户

- Android已安装到实体手机，内网连接由用户确认成功，验收完成，不再要求任何追加测试或内嵌授权。
- 提交已获授权：网页6a67a9e已推送master，其他改动按功能分组提交，不再为这些commit重复征求许可。Pages已于2026-09-24部署d4b3e0a，公开页面加载与主要资源哈希已核对通过；仅剩一次Pages公开入口连接游戏，不重开Safari功能测试。
- 仅当出现阻止启动或主要游戏功能的具体问题，且代理无法处理时，再说明阻塞及最短操作。

当前无需操作手机。Safari已完整验收完成；声音开关最后保持关闭，用户日常需要时自行打开，不再作为测试步骤。其他环境未覆盖的事实写入支持说明，不等于要求用户逐项补测。详见[精简矩阵](TEST_MATRIX.md)。
