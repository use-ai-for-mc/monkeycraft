import 'package:flutter/widgets.dart';

class GameContextMenuGuard extends StatelessWidget {
  const GameContextMenuGuard({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
