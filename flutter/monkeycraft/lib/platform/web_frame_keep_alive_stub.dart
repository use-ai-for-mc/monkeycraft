import 'package:flutter/widgets.dart';

class WebFrameKeepAlive extends StatelessWidget {
  final Widget child;

  const WebFrameKeepAlive({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
